import 'package:flutter/cupertino.dart';

class SettingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(''
          'Setting Screen',style: TextStyle(fontWeight: FontWeight.bold,fontSize: 30.0),
      ),
    );
  }
}
